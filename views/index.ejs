<% include partials/header %>
<div class="container">
    <% blogs.forEach(function(blog){ %>
        <!-- <div>
            <h2><%= blog.title %></h2>
            <img src="<%= blog.image %>">
            <span><%= blog.created %></span>
            <p><%= blog.body.substring(0, 100) %>...</p>
            <a href="/blogs/<%= blog._id %>">More</a>
        </div> -->
            <div class="container">
                <div id="blog" class="row align-items-center justify-content-center"> 
                    <div class="col-md-8">
                        <h1><%= blog.title %></h1>
                        <div class="container">
                            <img src="<%= blog.image %>" alt="post img" class="pull-left img-responsive thumb margin10 img-thumbnail">
                        </div>
                    </div>
                </div>     
                <div id="blog" class="row align-items-center justify-content-center">                  
                    <div class="col-md-8">                         
                        <article>
                            <p><%= blog.body.substring(0, 100) %>...</p>
                        </article>
                        <em>Created: <%= blog.created.toDateString() %></em><br>
                        <a class="btn btn-primary btn-sm" href="/blogs/<%= blog._id %>">READ MORE</a> 
                    </div>
                </div>
            </div>
        </div>
        <div class="container"><hr></div>
        
    <% }); %>








</div>    

<% include partials/footer %>

